# PC Monitor — залежності
# Обов'язкова:
psutil>=5.9

# Справжнє нативне вікно застосунку (без браузера).
# На Windows малює через вбудований WebView2 — окремий значок на панелі задач,
# власний процес, ніяких вкладок і адресного рядка.
pywebview>=5.0

# Windows-специфічні (тихо ігноруються на інших ОС):
pywintrace==0.2.0 ; sys_platform == "win32"   # точні байти мережі + DNS через ETW (потрібен адмін)
pystray>=0.19 ; sys_platform == "win32"        # іконка в треї
Pillow>=10.0 ; sys_platform == "win32"         # іконка для трея
