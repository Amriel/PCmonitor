# ── особисті дані користувача — НІКОЛИ не публікувати ──────────────────
# база: історія програм, усі відвідані домени та IP, запуски процесів
data/
# журнал монітора: шляхи з іменем користувача, виконані команди
logs/
# експортовані звіти: ім'я комп'ютера, повна поведінка програм
exports/
# особисті налаштування (файл створюється сам із типовими значеннями)
config.json
setup.log

# ── збірка ──────────────────────────────────────────────────────────────
build/
dist/
updates/
__pycache__/
*.pyc

# ── сліди трасування з робочої машини: перелік драйверів, назви пристроїв.
#    Продукт їх більше не пише сюди (лише в logs/), але правило лишається
#    страховкою — один такий файл уже колись доїхав до GitHub.
xperf_report*.txt
*.etl
*.etl.*

# ── тимчасове ───────────────────────────────────────────────────────────
_to_delete/
*.zip

# ── нотатки розробки з особистим контекстом (машина, робочі процеси) ────
DEVELOPMENT.md
